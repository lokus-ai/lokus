{
    "name": "{{pluginId}}",
    "version": "0.1.0",
    "description": "{{pluginDescription}}",
    "main": "dist/index.js",
    "scripts": {
        "build": "node build.mjs",
        "watch": "node build.mjs --watch",
        "test": "jest",
        "package": "lokus-plugin package",
        "prepublishOnly": "npm run build && npm run package"
    },
    "keywords": [
        "lokus",
        "plugin"
    ],
    "author": "{{pluginAuthor}}",
    "license": "MIT",
    "devDependencies": {
        "lokus-plugin-sdk": "^1.0.1",
        "esbuild": "^0.19.0",
        "typescript": "^5.0.0"
    }
}