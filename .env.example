# Authentication Backend URL
# Used for user authentication and OAuth flows
# Default: https://lokusmd.com (production)
# For local development, use: http://localhost:3000
VITE_AUTH_BASE_URL=https://lokusmd.com

# Google OAuth Credentials for Gmail Integration
#
# To set up Google OAuth:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select existing
# 3. Enable Gmail API and Google+ API
# 4. Go to "Credentials" → "Create Credentials" → "OAuth 2.0 Client IDs"
# 5. Application type: "Desktop application"
# 6. Add authorized redirect URI: http://localhost:8080/gmail-callback
# 7. Copy the Client ID and Client Secret below

GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Crash Reporting (Sentry/GlitchTip)
# Self-hosted at crash.lokusmd.com
# Get DSN from your GlitchTip dashboard
VITE_SENTRY_DSN=https://your-key@crash.lokusmd.com/project-id
TAURI_SENTRY_DSN=https://your-key@crash.lokusmd.com/project-id
VITE_SENTRY_ENVIRONMENT=production
VITE_ENABLE_CRASH_REPORTS=true

# Analytics (Umami)
# Self-hosted at analytics.lokusmd.com
# Get Website ID from Umami dashboard → Settings → Websites
VITE_UMAMI_WEBSITE_ID=your-website-id-here
VITE_UMAMI_API_ENDPOINT=https://analytics.lokusmd.com

# Note: Copy this file to .env and replace with your actual credentials
# Never commit the actual .env file with real credentials to git

# Set to 'true' for Mac App Store builds to disable in-app update checks
# (Apple Guideline 2.4.5 requires App Store apps to only update via the store)
VITE_DISABLE_UPDATE_CHECKER=false